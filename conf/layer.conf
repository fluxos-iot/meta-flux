BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
	${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "flux"
BBFILE_PATTERN_flux = "^${LAYERDIR}/"
BBFILE_PRIORITY_flux = "10"
